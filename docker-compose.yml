version: '3.8'

services:
  app:
    build: .
    container_name: clipy_app
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
